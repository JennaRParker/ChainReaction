<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/chainreaction/<%=chain._id%>?_method=PUT" method="POST">   
        <% for (i=0; i < chain.reaction.length; i++) { %>
        Order: <input type="text" name="order" value="<%=chain.reaction[i].order%>"> <br />
        Go Off: <textarea name="comment" value="<%=chain.reaction[i].comment%>"></textarea> <br />
        Rating <input type="range" name=rating value="<%=chain.reaction[i].rating%>" > <br />
        <input type="submit" value="Looks Up to Order" />
        <input type="reset" />
        <% } %>
    </form>
</body>
</html>